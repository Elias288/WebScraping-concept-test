<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Web scrapping github action</title>

    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        background-color: #eeeeee;
      }

      header {
        display: flex;
        align-items: center;
        justify-content: space-between;
        padding: 0.5rem 3rem;
        border-bottom: 1px solid #000;
      }

      #root {
        max-width: 800px;
        margin-inline: auto;
      }

      .postContent {
        background-color: #fff;
        margin-bottom: 2rem;
        border-radius: 10px;
        padding: 1rem 2rem;
      }
      .postContent:first-of-type {
        margin-top: 2rem;
      }

      .postContent img {
        aspect-ratio: 4/3;
        object-fit: contain;
        width: 250px;
      }
    </style>
  </head>

  <body>
    <header>
      <h1>Web scraping</h1>
      <p>Date of scrapping: <strong><%= creationDate %></strong></p>
    </header>

    <div id="root">
      <div class="postsContainer">
        <% items.forEach(function(item) { %> <%- include("partials/item", item)
        %> <% }); %>
      </div>
    </div>
  </body>
</html>
